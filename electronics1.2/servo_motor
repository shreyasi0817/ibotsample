#include <Servo.h>

Servo s;

void setup()
{
  s.attach(9);
}

void loop()
{
  for(int i=0;i<=180;i++)
  {
    s.write(i);
    delay(15);
  }

  for(int i=180;i>=0;i--)
  {
    s.write(i);
    delay(15);
  }
}
